<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1200 500" style="cursor:crosshair">
  <defs>
    <!-- Line glow — covers full canvas -->
    <filter id="glow" filterUnits="userSpaceOnUse" x="-30" y="-30" width="1260" height="560">
      <feGaussianBlur stdDeviation="3" result="b1"/>
      <feGaussianBlur stdDeviation="6" result="b2"/>
      <feMerge><feMergeNode in="b2"/><feMergeNode in="b1"/><feMergeNode in="SourceGraphic"/></feMerge>
    </filter>

    <!-- Center burst bloom -->
    <filter id="centerGlow" filterUnits="userSpaceOnUse" x="450" y="0" width="600" height="500">
      <feGaussianBlur stdDeviation="6"  result="b1"/>
      <feGaussianBlur stdDeviation="14" result="b2"/>
      <feGaussianBlur stdDeviation="28" result="b3"/>
      <feMerge><feMergeNode in="b3"/><feMergeNode in="b2"/><feMergeNode in="b1"/><feMergeNode in="SourceGraphic"/></feMerge>
    </filter>

    <!-- Wide halo around center -->
    <filter id="haloFilter" filterUnits="userSpaceOnUse" x="370" y="-100" width="900" height="700">
      <feGaussianBlur stdDeviation="22" result="b1"/>
      <feGaussianBlur stdDeviation="45" result="b2"/>
      <feMerge><feMergeNode in="b2"/><feMergeNode in="b1"/><feMergeNode in="SourceGraphic"/></feMerge>
    </filter>

    <!-- Beam glow -->
    <filter id="beamGlow" filterUnits="userSpaceOnUse" x="-10" y="215" width="790" height="70">
      <feGaussianBlur stdDeviation="2"  result="b1"/>
      <feGaussianBlur stdDeviation="5"  result="b2"/>
      <feGaussianBlur stdDeviation="10" result="b3"/>
      <feMerge><feMergeNode in="b3"/><feMergeNode in="b2"/><feMergeNode in="b1"/><feMergeNode in="SourceGraphic"/></feMerge>
    </filter>

    <!-- Dot glow — covers full canvas -->
    <filter id="dotGlow" filterUnits="userSpaceOnUse" x="-30" y="-30" width="1260" height="560">
      <feGaussianBlur stdDeviation="3" result="b1"/>
      <feGaussianBlur stdDeviation="6" result="b2"/>
      <feMerge><feMergeNode in="b2"/><feMergeNode in="b1"/><feMergeNode in="SourceGraphic"/></feMerge>
    </filter>

    <!-- Beam gradient: transparent left → white at center (x=750) -->
    <linearGradient id="beamGrad" x1="0%" y1="0%" x2="100%" y2="0%">
      <stop offset="0%"   stop-color="#0044aa" stop-opacity="0"/>
      <stop offset="40%"  stop-color="#1a6fff" stop-opacity="0.6"/>
      <stop offset="88%"  stop-color="#55aaff" stop-opacity="0.9"/>
      <stop offset="100%" stop-color="#ffffff"  stop-opacity="1"/>
    </linearGradient>
  </defs>

  <!-- Center: (750, 250) — 62.5% from left, vertically centered -->

  <!-- Outer halo — breathes via JS -->
  <circle id="haloOuter" cx="750" cy="250" r="110" fill="#0033aa" opacity="0.06" filter="url(#haloFilter)"/>
  <circle id="haloInner" cx="750" cy="250" r="58"  fill="#1155dd" opacity="0.09" filter="url(#haloFilter)"/>

  <!-- Incoming beam: x=0 → center (750) -->
  <rect x="0" y="248.5" width="750" height="3" fill="url(#beamGrad)" filter="url(#beamGlow)"/>
  <rect x="60" y="249.5" width="690" height="1" fill="#aaddff" opacity="0.9" filter="url(#beamGlow)"/>

  <!-- Rays — irregular angles and lengths, all pointing right -->
  <line id="line0" x1="750" y1="250" x2="778" y2="105" stroke="#3388ff" stroke-width="1" opacity="0.75" filter="url(#glow)"/>
  <line id="line1" x1="750" y1="250" x2="849" y2="56"  stroke="#3388ff" stroke-width="1" opacity="0.75" filter="url(#glow)"/>
  <line id="line2" x1="750" y1="250" x2="858" y2="116" stroke="#3388ff" stroke-width="1" opacity="0.75" filter="url(#glow)"/>
  <line id="line3" x1="750" y1="250" x2="966" y2="135" stroke="#3388ff" stroke-width="1" opacity="0.75" filter="url(#glow)"/>
  <line id="line4" x1="750" y1="250" x2="938" y2="224" stroke="#3388ff" stroke-width="1" opacity="0.75" filter="url(#glow)"/>
  <line id="line5" x1="750" y1="250" x2="968" y2="317" stroke="#3388ff" stroke-width="1" opacity="0.75" filter="url(#glow)"/>
  <line id="line6" x1="750" y1="250" x2="925" y2="336" stroke="#3388ff" stroke-width="1" opacity="0.75" filter="url(#glow)"/>
  <line id="line7" x1="750" y1="250" x2="862" y2="362" stroke="#3388ff" stroke-width="1" opacity="0.75" filter="url(#glow)"/>
  <line id="line8" x1="750" y1="250" x2="852" y2="451" stroke="#3388ff" stroke-width="1" opacity="0.75" filter="url(#glow)"/>
  <line id="line9" x1="750" y1="250" x2="782" y2="399" stroke="#3388ff" stroke-width="1" opacity="0.75" filter="url(#glow)"/>

  <!-- Endpoint dots -->
  <circle id="dot0" cx="778" cy="105" r="3.5" fill="#88ccff" filter="url(#dotGlow)"/>
  <circle id="dot1" cx="849" cy="56"  r="3.5" fill="#88ccff" filter="url(#dotGlow)"/>
  <circle id="dot2" cx="858" cy="116" r="3.5" fill="#88ccff" filter="url(#dotGlow)"/>
  <circle id="dot3" cx="966" cy="135" r="3.5" fill="#88ccff" filter="url(#dotGlow)"/>
  <circle id="dot4" cx="938" cy="224" r="3.5" fill="#88ccff" filter="url(#dotGlow)"/>
  <circle id="dot5" cx="968" cy="317" r="3.5" fill="#88ccff" filter="url(#dotGlow)"/>
  <circle id="dot6" cx="925" cy="336" r="3.5" fill="#88ccff" filter="url(#dotGlow)"/>
  <circle id="dot7" cx="862" cy="362" r="3.5" fill="#88ccff" filter="url(#dotGlow)"/>
  <circle id="dot8" cx="852" cy="451" r="3.5" fill="#88ccff" filter="url(#dotGlow)"/>
  <circle id="dot9" cx="782" cy="399" r="3.5" fill="#88ccff" filter="url(#dotGlow)"/>

  <!-- Center burst — layered bloom, animated via JS -->
  <circle id="burst3" cx="750" cy="250" r="26"  fill="#0d3acc" opacity="0.28" filter="url(#centerGlow)"/>
  <circle id="burst2" cx="750" cy="250" r="14"  fill="#2255ee" opacity="0.55" filter="url(#centerGlow)"/>
  <circle id="burst1" cx="750" cy="250" r="6"   fill="#55aaff" opacity="0.90" filter="url(#centerGlow)"/>
  <circle              cx="750" cy="250" r="2.5" fill="#cce8ff" opacity="1"/>
  <circle              cx="750" cy="250" r="1"   fill="#ffffff"  opacity="1"/>

  <script type="text/javascript"><![CDATA[
  (function () {
    var CX = 750, CY = 250;

    var endpointDefs = [
      [778, 105], [849, 56],  [858, 116], [966, 135], [938, 224],
      [968, 317], [925, 336], [862, 362], [852, 451], [782, 399]
    ];

    var rays = endpointDefs.map(function (ep, i) {
      var dx = ep[0] - CX, dy = ep[1] - CY;
      return {
        angle:    Math.atan2(dy, dx),
        baseDist: Math.sqrt(dx * dx + dy * dy),
        phase:    (i / endpointDefs.length) * Math.PI * 2
      };
    });

    var lines = rays.map(function (_, i) { return document.getElementById('line' + i); });
    var dots  = rays.map(function (_, i) { return document.getElementById('dot'  + i); });

    var haloOuter = document.getElementById('haloOuter');
    var haloInner = document.getElementById('haloInner');
    var burst1    = document.getElementById('burst1');
    var burst2    = document.getElementById('burst2');
    var burst3    = document.getElementById('burst3');

    var pos = rays.map(function (r) {
      return { x: CX + r.baseDist * Math.cos(r.angle),
               y: CY + r.baseDist * Math.sin(r.angle) };
    });

    var mouseX = null, mouseY = null;

    var PULSE_PERIOD   = 10.0;
    var PULSE_AMP      = 0.18;
    var GLOW_PERIOD    = 5.0;
    var ATTRACT_RADIUS = 170;
    var ATTRACT_POWER  = 0.65;
    var LERP_NORMAL    = 1.5;
    var LERP_ATTRACT   = 5.0;

    var svg = document.querySelector('svg');
    svg.addEventListener('mousemove', function (e) {
      var r = svg.getBoundingClientRect();
      mouseX = (e.clientX - r.left) * (1200 / r.width);
      mouseY = (e.clientY - r.top)  * (500  / r.height);
    });
    svg.addEventListener('mouseleave', function () { mouseX = null; mouseY = null; });

    var lastTs = null, elapsed = 0;

    function frame(ts) {
      if (lastTs === null) lastTs = ts;
      var dt = Math.min((ts - lastTs) / 1000, 0.05);
      lastTs = ts;
      elapsed += dt;

      var glowT = 0.5 + 0.5 * Math.sin((elapsed / GLOW_PERIOD) * Math.PI * 2 + 1.2);
      haloOuter.setAttribute('opacity', 0.04 + 0.05 * glowT);
      haloInner.setAttribute('opacity', 0.07 + 0.08 * glowT);
      burst3.setAttribute('opacity',    0.20 + 0.18 * glowT);
      burst2.setAttribute('opacity',    0.45 + 0.25 * glowT);
      burst1.setAttribute('opacity',    0.80 + 0.18 * glowT);

      var angFreq = (2 * Math.PI) / PULSE_PERIOD;

      var closestIdx = -1, closestDist = Infinity;
      if (mouseX !== null) {
        for (var i = 0; i < rays.length; i++) {
          var d = Math.sqrt(Math.pow(mouseX - pos[i].x, 2) + Math.pow(mouseY - pos[i].y, 2));
          if (d < closestDist) { closestDist = d; closestIdx = i; }
        }
        if (closestDist > ATTRACT_RADIUS) closestIdx = -1;
      }

      for (var i = 0; i < rays.length; i++) {
        var r = rays[i];
        var pulse = 1 + PULSE_AMP * Math.sin(elapsed * angFreq + r.phase);
        var dist  = r.baseDist * pulse;

        var tx = CX + dist * Math.cos(r.angle);
        var ty = CY + dist * Math.sin(r.angle);

        if (i === closestIdx) {
          var af = ATTRACT_POWER * Math.max(0, 1 - closestDist / ATTRACT_RADIUS);
          tx += (mouseX - tx) * af;
          ty += (mouseY - ty) * af;
        }

        var k = Math.min(1, ((i === closestIdx) ? LERP_ATTRACT : LERP_NORMAL) * dt);
        pos[i].x += (tx - pos[i].x) * k;
        pos[i].y += (ty - pos[i].y) * k;

        lines[i].setAttribute('x2', pos[i].x);
        lines[i].setAttribute('y2', pos[i].y);
        dots[i].setAttribute('cx', pos[i].x);
        dots[i].setAttribute('cy', pos[i].y);
      }

      requestAnimationFrame(frame);
    }

    requestAnimationFrame(frame);
  })();
  ]]></script>
</svg>
